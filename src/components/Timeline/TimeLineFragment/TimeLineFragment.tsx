import { TimeLineFragment } from '~api/editorData';

import styles from './TimeLineFragment.module.css';

const TimeLineFragment = ({ name }: TimeLineFragment) => {
  return <div></div>;
};

export default TimeLineFragment;
